**Configuration**: config/config.yaml

- output location

**Build**\
`docker compose build`

**Run pipeline**\
`docker compose run --rm -e INPUT_FILE_NAME={file_path} pipeline`

**Run api**\
`docker compose up -d api`

[http://localhost:8000/docs](http://localhost:8000/docs)
